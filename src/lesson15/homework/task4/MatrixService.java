public class MatrixService {
    UserInput4 ui = new UserInput4();
    private int n = ui.uiInt("insert matrix size: ");
    private task4.Matrix matrix = new task4.Matrix(n,new int[n][n]);
    private int[][] fillMatrix(){
        for (int i = 0; i < n; i++) {
            
        }
    }
}
